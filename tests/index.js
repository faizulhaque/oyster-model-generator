"use strict";

var chai = require("chai"),
    expect = chai.expect;

describe("Validations", function() {

    beforeEach(function () {

    });

    afterEach(function() {

    });

    describe("Should pass for every it...", function () {
        it("1 === 1", function () {
            expect(1).to.equal(1);
        });
    });
});
